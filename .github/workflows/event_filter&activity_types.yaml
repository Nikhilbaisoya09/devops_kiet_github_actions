name: Event Filter and Activity Types

on:
#   pull_request:
#     types: opened
#     branches:
#         - main
    workflow_dispatch:
    
jobs:
    echo:
        runs-on: ubuntu-latest
        steps:
            - run: echo "Running whenever a pull request is opened on the main branch"